import { ArrayAsTextPipe } from './array-as-text.pipe';

describe('ArrayAsTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayAsTextPipe();
    expect(pipe).toBeTruthy();
  });
});
