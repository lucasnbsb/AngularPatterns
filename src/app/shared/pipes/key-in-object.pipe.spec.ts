import { KeyInObjectPipe } from './key-in-object.pipe';

describe('KeyInObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyInObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
