import { EnumMatchPipe } from './enum-match.pipe';

describe('EnumMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
