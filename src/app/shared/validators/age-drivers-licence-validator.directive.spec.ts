import { AgeDriversLicenceValidatorDirective } from './age-drivers-licence-validator.directive';

describe('AgeDriversLicenceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeDriversLicenceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
